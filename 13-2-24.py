"""
#Remove:

*Removes the item with the specified value.
*Value would be delete.
*Required a parameter.
*If no parameters is given, error occurs.
*If the given parameter is not present in the set, error occurs - value error.

#Pop:

*Removes the element at the specified position.
*Value would be return.
*Does not require any parameter.
*It returns last element (when we not given any parameter).
*If the parameter which is given is freater than length of list then it raise out og range error.

#Discard:

*Remove the specified item.
*If the given parameter is not in the set, no errors, just print what is in the set.
*It does not raise the error.
*It is different from the remove method but in discard method will not raise the error.

"""